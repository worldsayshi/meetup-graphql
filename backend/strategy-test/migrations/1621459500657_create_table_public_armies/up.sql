CREATE TABLE "public"."armies"("id" serial NOT NULL, "current_node" integer NOT NULL, "size" integer NOT NULL, "planned_path" jsonb NOT NULL DEFAULT '[]'::jsonb, "team" integer NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("current_node") REFERENCES "public"."nodes"("id") ON UPDATE restrict ON DELETE restrict); COMMENT ON TABLE "public"."armies" IS E'armies that travel around the map and maybe fight each other';
